spring:
  jpa:
    hibernate:
      ddl-auto: create
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    url: "jdbc:postgresql://db:5432/springbootjpa"
    username: postgres
    password: docker

logging:
  level:
    org:
      springframework:
        security: DEBUG